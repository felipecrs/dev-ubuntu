name: Manual Release

on:
  repository_dispatch:
    types: manual-release
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          git commit --allow-empty -m "build(deps): manual release trigger"
          git push origin master
